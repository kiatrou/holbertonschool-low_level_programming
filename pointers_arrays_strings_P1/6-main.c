#include <stdio.h>
#include "main.h"
/**
 * main - prints every other character of
 * a string
 *
 * Return: Always 0 (Success)
*/
int main(void)
{
	char *str;

	str = "0123456789";
	puts2(str);
	return (0);
}
